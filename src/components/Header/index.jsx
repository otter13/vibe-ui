import PropTypes from 'prop-types'
import './index.less'

/**
 * Header - Head component
 * @param {themeColor} string The theme color
 */
function Header(props) {
  return <div className="headerWrap">
    Header
  </div>
}

Header.propTypes = {

}

export default Header

